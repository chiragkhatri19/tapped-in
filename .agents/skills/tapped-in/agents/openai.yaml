interface:
  display_name: "Tapped In"
  short_description: "Repo-specific patterns and workflows for tapped-in"
  default_prompt: "Use the tapped-in repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true